extends ../layout/common
block content
    h1 Danh sách user
    a(href="/users/create") Create
    form(action='/users/search', method='GET', class='d-flex')
        input(class='form-control mr-2 ',type='text',name='q',value=keyword)
        button(class='btn btn-secondary btn-sm') Search
    ol
        each user in users
            li= user.name 
            span &nbsp 
            a(href="/users/" + user.id) Xem chi tiết